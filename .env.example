# Example environment configuration for A Life Worth Celebrating

NODE_ENV=development
PORT=3000

# PostgreSQL connection URL
# For Heroku, this is automatically provided as DATABASE_URL when you add the Postgres addon.
DATABASE_URL=postgres://USERNAME:PASSWORD@HOST:PORT/DATABASE

# JWT secret used to sign authentication tokens
JWT_SECRET=your-super-secret-jwt-key

# Main admin account (seeded on server start if not present)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=very-secure-password
ADMIN_NAME=Main Admin

# AWS S3 Configuration for image uploads
AWS_REGION=us-east-2
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_S3_BUCKET=your-bucket-name
S3_ACCESS_POINT_ARN=arn:aws:s3:region:account-id:accesspoint/access-point-name
